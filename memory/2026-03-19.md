# 2026-03-19

- Started planning a new OpenClaw project: **Mission Control**.
- Core concept: a web-based control plane for OpenClaw with an operational dashboard plus an **Office View** where agents appear at tables/desks and their current tasks, status, sessions, and activity are visible spatially.
- Initial product direction: observability first, lightweight orchestration second.
- Recommended stack so far: **TypeScript + Node.js backend**, likely **Fastify**, with **WebSocket** for realtime updates and **PostgreSQL** for persistence if built as a serious product.
- Produced a v1 spec covering Overview, Office, Sessions, Tasks, Events, control actions, domain model, API shape, realtime protocol, and phased delivery.
- Produced follow-up technical artifacts in `/home/node/.openclaw/workspace/mission-control/`: shared TypeScript contracts (`contracts/domain.ts`, `contracts/api.ts`, `contracts/ws.ts`), monorepo structure doc, ASCII wireframes, and an implementation checklist.
